import { ShowCardsDirective} from './show-cards.directive';
import {Renderer2, ElementRef} from '@angular/core'

describe('ShowCardsDirective', () => {
  it('should create an instance', () => {
  });
});
